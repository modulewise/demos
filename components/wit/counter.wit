/// the counter interface
interface counter {
    /// count things
    count: func(thing: string) -> string;
}

/// the counter world
world counter-world {
    import wasi:config/store@0.2.0-rc.1;
    import incrementor;
    export counter;
}
